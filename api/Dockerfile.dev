###################
# BUILD FOR LOCAL DEVELOPMENT
###################

FROM node:18-alpine
ENV DATABASE_URL $DATABASE_URL
WORKDIR /app

COPY . /app

RUN npm install

CMD [ "npx", "prisma", "migrate", "dev" ]
EXPOSE 3000
