import { Injectable } from '@nestjs/common';

@Injectable()
export class WeekplanService {
    getWeekPlan() {
        //Take the Preferences and search for receipes
        //Take seven of them and add weekday based on actual day to it return them
        return '';
    }
}
